-- This script computes the average score of all records in the "second_table" table

-- Compute the average score from the "second_table" table
-- Alias the result as "average"
SELECT AVG(score) AS average FROM second_table;
