-- This script creates a table in the database and adds columns

-- Create the "first_table" table if it doesn't already exist
CREATE TABLE IF NOT EXISTS first_table (
  id INT,
  name VARCHAR(256)
);
